import * as React from 'react';
import * as style from './style.scss';

const FooterView = (props) => {
  return <p className={style.right}>
    Copyright Starky's Club
  </p>;
};

export default FooterView;